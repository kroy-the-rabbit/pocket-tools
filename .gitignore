cheatgui/.venv/
__pycache__/
*.pyc

# PyInstaller output
/build/
/dist/

# Private half of the release signing key, made by
# packaging/make-release-key.sh. Never commit this.
/.release-key/

# Built by packaging/make-icons.sh on macOS only; the iconset it comes from is
# committed, so CI makes this itself.
/assets/icon.icns

# Worktrees made for parallel agent runs. Transient, and not ours to commit.
/.claude/
